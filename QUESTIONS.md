# Вопросы по спеке и ответы

История уточнений к версии 2 спеки. Вопросы 1–107 заданы по тексту v2,
108–118 возникли из ответов на них. Все ответы получены от владельца и сведены
в [SPEC.md](SPEC.md) версии 3 — этот файл справочный, при расхождении
приоритет у SPEC.md.

Метка `[БЛОКЕР]` означала «без ответа не двигаюсь дальше»; на момент версии 3
блокеров не осталось.

---

## A. Доступ, окружение, процесс

**1. [БЛОКЕР]** Кто фактически исполняет код на плате? Написать `bench.py` могу,
собрать whisper.cpp на RK3588 и померить RTF/температуру — нет. Этап 0
блокирующий и целиком про железо.
**Ответ.** Владелец может прогонять сам, но предпочитает дать доступ к плате;
на плате стоит netbird VPN. Итог: Claude Code запускается прямо на плате, эта
облачная сессия для этапа 0 не используется.

**2.** То же по станции с A5000.
**Ответ.** Аналогично п. 1.

**3.** Версия Python на плате? От неё зависит `tomllib` (3.11+).
**Ответ.** 3.12.3, `tomllib` доступен.

**4. [БЛОКЕР]** `glossary.yaml`: в stdlib нет YAML-парсера, PyYAML противоречит
«внешние пакеты — только numpy».
**Ответ.** Глоссарий переводится в JSON. Формат — §7.2 SPEC.md.

**5. [БЛОКЕР]** sherpa-onnx: Python-колесо или CLI-бинарники из релизов?
**Ответ.** CLI-бинарник, вывод парсим. Python-биндинги не используем.

**6.** numpy из apt или pip в venv? venv заводим?
**Ответ.** Из apt, уже установлен. Всё в системном питоне: venv не заводим,
диск платы маленький.

**7.** Один `asr.py` + симлинк, дробить после ~1000 строк?
**Ответ.** Да.

**8.** Репозиторий `aura-asr`, пути `/srv/asr`, команда `asr` — так и оставляем?
**Ответ.** Пути скорректируются по ходу реализации, остальное да. В SPEC.md
корневой каталог задаётся в `config.toml`, `/srv/asr` — только пример.

**9.** Язык кода, комментариев, ошибок, коммитов?
**Ответ.** Русский.

**10.** Сколько ресурсов платы можно занимать?
**Ответ.** Всё, кроме одного ядра — его оставить ОС.

**11.** Нужен ли CI?
**Ответ.** Нужен, работаем по TDD. Детали — вопрос 108.

**12.** Под каким пользователем systemd-юниты и владение каталогом?
**Ответ.** Всё под root.

## B. Данные, глоссарий, эталон

**13. [БЛОКЕР]** Есть ли запись созвона и размеченный эталон?
**Ответ.** Запись есть, транскрипции и разметки нет. Размечает владелец.

**14. [БЛОКЕР]** Формат эталона.
**Ответ.** Формат задаёт исполнитель, владелец размечает по нему. Принят
`gold.tsv`, совместимый с экспортом меток Audacity — §7.3 SPEC.md. Дословный
текст только внутри `wer_window`, остальной созвон — только разметка говорящих.

**15.** Глоссарий существует? Порядок величины?
**Ответ.** Не существует, терминов много. Наполняется итеративно: кандидаты
собираются автоматически после первого прогона, владелец вычитывает.

**16. [БЛОКЕР]** Только каноны или канон + искажения?
**Ответ.** Канон + список искажений (`variants`).

**17.** Как термин выглядит в тексте? Склонения?
**Ответ.** Как правило латиницей, нормализуем к канону, склонения убираем.

**18.** Правила нормализации для WER: регистр, пунктуация, числа, дефисы.
**Ответ.** Числа всегда цифрами («пять» → «5»). Дефисы значимы: пропуск или
лишний дефис — ошибка. По регистру исходный ответ был исправлен, см. вопрос 111.

**19.** Метрика по терминам; как считать «VDA 5050» вместо «VDA5050»?
**Ответ.** Это искажение, постобработка обязана его склеить. Метрика — F1.

**20.** Есть ли архив записанных созвонов?
**Ответ.** Созвоны есть, транскрибации нет.

## C. Схема записи, манифест, ingest

**21.** Кто пишет `manifest.json`?
**Ответ.** Сама утилита — интерактивно либо из флагов. Руками не пишется.

**22.** `call_id` задаёт человек или каталог? Коллизии?
**Ответ.** Задаёт человек. При коллизии — ошибка, обработка не продолжается.

**23.** reference — всегда stream 0 того же файла?
**Ответ.** Да.

**24.** Если дорожки в OBS перепутаны местами?
**Ответ.** Проверить и предупредить, но верить манифесту.

**25.** Участник в `attendees` без enrollment?
**Ответ.** Ошибка.

**26.** Часовые пояса; откуда взялось `created_at`?
**Ответ.** Приводить к UTC+3. `created_at` — колонка из схемы `segments` в §7
спеки v2, время записи строки в БД.

**27.** Что ingest делает с исходниками?
**Ответ.** Копирует и валидирует.

**28.** Дисковый бюджет, храним ли wav вечно?
**Ответ.** Диск есть, но созвон лучше паковать в архив `<call_id>.call`.
Принято: tar с FLAC внутри.

**29.** Битый файл: валим созвон или продолжаем?
**Ответ.** Если это не опорная дорожка — просто предупреждение.

**30.** Нет опорной дорожки — отказ?
**Ответ.** Отказ подтверждён.

**31.** Две опорные дорожки?
**Ответ.** Первая в манифесте.

**32.** Проверен ли фактический формат записи Толка?
**Ответ.** Не проверен, владелец готов проверить. Нужны: вывод
`ffprobe -v error -show_format -show_streams -of json <файл>`, ОС и версия
приложения, путь сохранения по умолчанию, наличие опции «дорожки участников
отдельно».

## D. Этап 0 — замер

**33.** Флаги сборки whisper.cpp? Собран ли уже?
**Ответ.** Ничего не собрано. Флаги — те, при которых на плате быстрее всего;
подбираются замером.

**34.** Фиксируем тег whisper.cpp?
**Ответ.** Фиксируем, последний стабильный.

**35.** Добавлять ли Q8_0 к Q5_0?
**Ответ.** Добавить всё, что исполнитель сочтёт нужным.

**36.** `-t 4` — просто потоки или пин на большие ядра?
**Ответ.** Померить оба варианта и выбрать лучший.

**37.** Даёт ли `--flash-attn` что-то на CPU?
**Ответ.** Проверить замером, а не принимать на веру.

**38. [БЛОКЕР]** Мерить на цельном файле или на нарезке по VAD?
**Ответ.** По нарезке от VAD. Модель при этом не перезагружается — нужен кеш,
см. вопрос 47.

**39.** RTF = wall_clock / длительность аудио?
**Ответ.** Да.

**40.** Как мерить peak RSS?
**Ответ.** На усмотрение исполнителя.

**41.** Какие thermal_zone за что отвечают?
**Ответ.** Логировать все.

**42.** Какой governor?
**Ответ.** Вроде `performance`, но лучше перевыставить принудительно.

**43.** Своя реализация WER или `jiwer`?
**Ответ.** `jiwer` можно — только для `bench.py`, в пайплайн не тащим.

**44.** Кто принимает решение по итогам замера?
**Ответ.** Владелец, по таблице.

**45.** Есть ли потолок латентности для tier=1?
**Ответ.** Нет.

**46.** Состояние станции: стоит ли faster-whisper, версии, можно ли ставить пакеты?
**Ответ.** Не стоит, ставить пакеты можно. CUDA 13.2, драйвер 595.71.05.

## E. Этап 1 — ночной контур

**47. [БЛОКЕР]** Как whisper.cpp получает аудио: цельный файл со своим VAD,
посегментные запуски или конкатенация в чанки?
**Ответ.** Посегментно, но с кешированием модели. Реализация: whisper.cpp в
режиме `whisper-server`, сегменты уходят по HTTP, модель грузится один раз.

**48.** Параметры VAD?
**Ответ.** Подбираются исполнителем по эталону, живут в конфиге.

**49.** Длинный монолог — по паузам или жёстко по 30 с?
**Ответ.** По паузам.

**50.** Таймкоды по VAD или по whisper? Нужны ли пословные?
**Ответ.** По VAD; пословные тоже включить.

**51.** Язык жёстко `ru`?
**Ответ.** Всегда русский, но с английскими вставками — термины и короткие
цитаты из документации.

**52.** Промпт собирать автоматически или писать руками?
**Ответ.** Руками, в конфиге.

**53.** Фильтровать ли галлюцинации?
**Ответ.** Пока не добавляем, посмотрим по ходу эксплуатации.

**54.** Сохранять ли `text_raw`?
**Ответ.** Сохранять.

**55.** Порог `difflib`, стоп-лист, n-граммы?
**Ответ.** Подбирать по качеству метрик.

**56.** Хранить t0/t1 в целых миллисекундах?
**Ответ.** Согласен.

**57.** Повторный `transcribe` на посчитанном созвоне?
**Ответ.** Пересчёт с заменой. Уточнено в вопросе 114: пересчитывать только
если сменились модель или глоссарий.

**58.** Набор состояний `calls.status`?
**Ответ.** Предложенный исполнителем.

**59.** Добавлять ли `status`/`error` в `tracks`?
**Ответ.** Добавить.

**60.** Экспорт: перезапись целиком, атомарно?
**Ответ.** Да и да.

**61.** Что писать в `speaker` на этапе 1?
**Ответ.** `unknown`.

**62.** Достаточно ли journald?
**Ответ.** Достаточно.

**63.** Блокировка на систему или на созвон?
**Ответ.** На созвон.

**64.** Маркер готовности для `.path`-юнита — какие есть варианты?
**Ответ.** Владелец попросил описать варианты. Разобраны четыре: манифест
последним + `PathExistsGlob`, отдельный `READY`, карантин по mtime, отказ от
`.path` в пользу только ночного таймера. Принято: основа — `.timer`, `.path`
опционально; манифест пишется последним.

**65.** Время ночного прогона?
**Ответ.** Вынести в конфиг.

## F. Схема БД и контракт с агентами

**66. [БЛОКЕР]** Как выражается вытеснение при `reprocess`?
**Ответ.** Добавить колонку `run_id`. В схему добавлены также `active` и
таблица `runs`.

**67.** Хранить ли метрики уверенности и similarity-скор?
**Ответ.** Хранить.

**68.** Хранить ли версию глоссария и промпта?
**Ответ.** Хранить.

**69.** Стабильны ли `segments.id` между перегонами?
**Ответ.** Пусть будут стабильны.

**70. [БЛОКЕР]** Как агенты узнают, что созвон готов?
**Ответ.** Владелец скажет им «прочитай». Отдельный маркер готовности не нужен.

**71.** В JSONL `speaker` — id или display_name?
**Ответ.** `display_name`.

**72. [БЛОКЕР]** Дедупликация речи оператора между `obs_mic` и `reference`?
**Ответ.** Оставлять один текст, порог перекрытия подбирать по эталону.

**73.** Миграции схемы?
**Ответ.** Пересоздаём БД, миграций не пишем.

**74.** Бэкап `db.sqlite`?
**Ответ.** Не нужен.

## G. Этап 1.5 — A5000

**75.** Параметры станции, кто разворачивает окружение?
**Ответ.** `etu-pc-1`, root, Ubuntu 24.04. Развернуть всё с нуля, окружение —
виртуальное (venv на станции заводим, в отличие от платы).

**76.** Что едет на станцию: сегменты или дорожка целиком?
**Ответ.** Без нарезки, дорожка целиком.

**77.** `python -c` или отдельный скрипт на станции?
**Ответ.** Запускать отдельной командой.

**78.** Сеть между платой и станцией?
**Ответ.** ≈78 Мбит/с.

**79.** Что делать, если обе карты заняты частично?
**Ответ.** Карта на машине одна, вопрос снимается.

**80.** К чему относится `timeout_sec`?
**Ответ.** Ключ взят из §6 спеки v2. Смысл задан исполнителем: максимальное
время одной удалённой команды ASR на одну дорожку, дефолт 3600.

**81.** Разрешено ли досчитывать локально после обрыва, оставляя GPU-сегменты?
**Ответ.** Да. Реализовано на уровне дорожки: дорожка целиком либо GPU, либо
локально, разные дорожки одного созвона могут различаться.

**82.** По какому полю фильтрует `--since`, что именно пересчитывается?
**Ответ.** Фильтр по `calls.started_at` подтверждён; пересчёт зависит от того,
насколько устарел сегментный слой — правило детализировано в вопросе 114.

**83.** Кто настраивает ssh-ключ с `command=`?
**Ответ.** Администратор, по инструкции от исполнителя.

## H. Этап 2 — атрибуция

**84.** Какую модель эмбеддингов брать?
**Ответ.** Выбрать замером.

**85.** Кто размечает созвон для подбора порога `unknown`?
**Ответ.** Владелец, на том же эталоне.

**86.** Один центроид или несколько эмбеддингов на человека?
**Ответ.** Несколько, раздельно по каналам.

**87.** Автопополнение базы из `obs_mic`?
**Ответ.** Не нужно.

**88. [БЛОКЕР]** Как назначать говорящего ASR-сегменту при несовпадении границ?
**Ответ.** По максимальному перекрытию. Утверждение «резать текст нельзя»
исполнителем снято: с пословными таймкодами (п. 50) сегмент режется по границе
слова. Не режется только настоящее наложение двух голосов.

**89.** Игнорировать ли перекрывающуюся речь?
**Ответ.** Размечаем, pyannote для этого разрешён.

**90. [БЛОКЕР]** Фиксированное число кластеров конфликтует с `unknown`.
**Ответ.** Вводные: кто-то из списка может молчать — это нормально;
незаявленные гости не приходят. Отсюда решение исполнителя: число кластеров
не фиксируем, кластеризация по порогу, метка кластера — ближайший центроид
среди `attendees`, иначе `unknown`.

**91.** Исключать ли из диаризации интервалы, закрытые дорожкой `obs_mic`?
**Ответ.** Встречный вопрос владельца: а если в дорожке всё равно несколько
человек? Решение: вслепую не вычитаем. Участок засчитывается оператору, только
если согласны и VAD, и эмбеддинг; остальное уходит в общую идентификацию.

**92.** Где используется `display_name`?
**Ответ.** В JSONL, его читает агент.

**93.** Считать долю атрибуции по сегментам или по времени?
**Ответ.** По времени.

## I. Этап 2.5 — сольные дорожки

**94.** Параметры кросс-корреляции, отбраковка выбросов?
**Ответ.** На усмотрение исполнителя, лишь бы работало устойчиво.

**95.** Обрезать ли сольную дорожку по опорному таймлайну?
**Ответ.** Обрезать.

**96.** Можно ли использовать ffmpeg в self-check со сдвигом и растяжением?
**Ответ.** Можно.

**97.** Что делать, если корреляция не сошлась?
**Ответ.** Различать два случая: человек молчал — дорожка просто пустая, это
не ошибка; дорожка от другого созвона — это ошибка.

## J. Этап 3 — живой контур

**98. [БЛОКЕР]** Откуда плата берёт живой звук?
**Ответ.** Отдельный стрим с машины оператора.

**99.** Как живой контур узнаёт `call_id` до появления манифеста?
**Ответ.** Временный созвон, сшивается с ночным прогоном.

**100.** Кто потребитель tier=1?
**Ответ.** Оператор: получает стрим текста в консоль.

**101.** Сервис постоянный или на время созвона?
**Ответ.** Запускается только на время созвона.

## K. Профилирование, эксплуатация, прочее

**102.** Тайминги по стадиям — в БД или в лог?
**Ответ.** В отдельную таблицу БД (`timings`).

**103.** Требования по приватности и хранению?
**Ответ.** Не актуальны, каталог владелец назначит сам.

**104.** Откуда брать `started_at`?
**Ответ.** Из манифеста, при отсутствии — из mtime.

**105.** Формат self-check'ов?
**Ответ.** Владельцу термин был непонятен: он из §9 спеки v2, где требовались
одиночные `assert`-блоки без фреймворков. Заменено на нормальные тесты,
см. вопрос 108.

**106.** Куда фиксировать результаты приёмки этапов?
**Ответ.** В чат. Отдельного файла в репозитории не заводим.

**107.** Уведомления о падении ночного прогона?
**Ответ.** Только journald.

---

# Вопросы 108–118

Возникли из ответов на первые 107.

**108.** TDD (п. 11) противоречит §9 спеки v2, требующей одиночные `assert`-блоки
без фреймворков. Предложение: `unittest` из stdlib — формально фреймворк, но
нулевая зависимость; CI гоняет x86-совместимую часть.
**Ответ.** Да, так и делаем.

**109.** Что кладём в CI, если нет ни моделей, ни платы: только чистую логику или
тестовый wav и модель `tiny`?
**Ответ.** В CI только юнит-тесты.

**110.** Куда девается каталог из `incoming/` после ingest? Иначе `.path`-юнит
будет стрелять по кругу.
**Ответ.** Удалять.

**111.** Регистр: п. 18 говорит «строчный если аббревиатура», п. 17 — «термин
латиницей» (`MQTT`). Как правильно?
**Ответ.** В п. 18 была ошибка: аббревиатуры прописными.

**112.** Живой контур: какая ОС у оператора и чем стримить? Подходит ли OBS
Custom Output (FFmpeg), mpegts по UDP через netbird?
**Ответ.** Подходит. Живой контур нужен для потенциальной интеграции агентов
прямо на созвоны — чтобы голосовыми командами управлять агентами-кодогенераторами.

**113.** Оператор смотрит текст по ssh с платы или текст гонится обратно на его
машину?
**Ответ.** Обратно на машину, командой вида `asr receive`.

**114.** Подтвердить фильтр `--since` по `calls.started_at` и нужен ли флаг
`--only-glossary`.
**Ответ.** Фильтр подтверждён. Глоссарий переприменять только если он обновился;
tier=2 не пересчитывать, если запись уже есть и версия модели не менялась.

**115.** CUDA 13.2 и cuDNN 9: если готового колеса CTranslate2 не окажется —
откат на CUDA 12 или whisper.cpp с CUDA?
**Ответ.** На CUDA 12 не откатываемся, тогда whisper.cpp.

**116.** Несколько эмбеддингов на человека не влезают в `speakers.embedding` —
нужна таблица `speaker_embeddings`. Подтверждаете расширение схемы?
**Ответ.** Да.

**117.** Кто и когда пишет первые образцы enrollment?
**Ответ.** Владелец, после приёмки этапа 1.

**118.** Разметку делать двумя частями: 2 минуты дословно плюс весь созвон по
говорящим без текста?
**Ответ.** Да.
