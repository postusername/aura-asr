# Вопросы по ASRSPEC1 (v2)

Метка `[БЛОКЕР]` — без ответа не двигаюсь по соответствующему этапу (§10 спеки:
спрашивать, а не угадывать). У остальных, где возможно, указан дефолт, который
приму молчанием.

Нумерация сквозная — отвечать можно номерами.

---

## A. Доступ, окружение, процесс

1. **[БЛОКЕР]** Кто фактически исполняет код на плате? Я работаю в контейнере:
   написать `bench.py` могу, собрать whisper.cpp на RK3588 и померить
   RTF/температуру — нет. Этап 0 блокирующий и целиком про железо.
   Варианты: (а) вы прогоняете и присылаете вывод, (б) SSH к плате в сессию,
   (в) иначе.
2. То же по станции с A5000: доступ есть, или ручной замер `large-v3`/beam=5
   делаете вы?
3. Версия Python на плате? От неё зависит `tomllib` (3.11+) для `config.toml`.
   Если 3.10 — парсить конфиг руками или разрешён `tomli`?
4. **[БЛОКЕР]** `glossary.yaml`: в stdlib нет YAML-парсера, PyYAML противоречит
   «внешние пакеты — только numpy». Варианты: (а) сделать глоссарий TOML/JSON,
   (б) разрешить PyYAML, (в) парсить руками плоское подмножество YAML.
   Склоняюсь к (а) — формат всё равно наш, а `tomllib` уже нужен для конфига.
5. **[БЛОКЕР]** sherpa-onnx: ставим Python-колесо (`pip install sherpa-onnx`,
   aarch64-колёса есть) или используем только готовые CLI-бинарники из релизов
   и парсим их вывод? §6 говорит и «бинарники из релизов», и «только numpy» —
   для VAD/диаризации это разные пути.
6. numpy — из apt (`python3-numpy`) или pip в venv? venv вообще заводим или всё
   в системный питон?
7. Раскладка кода: один `asr.py` + симлинк в `/usr/local/bin/asr`? Дефолт —
   один файл до конца этапа 1, дроблю только когда перевалит ~1000 строк.
8. Репозиторий `aura-asr`, пути в спеке `/srv/asr`, команда `asr` — так и
   оставляем?
9. Язык кода: комментарии, `ponytail:`-пометки, тексты ошибок CLI, сообщения
   коммитов — русский или английский?
10. Что ещё крутится на плате? Ночной прогон может занимать все 8 ядер и 4+ ГБ,
    или надо оставлять запас?
11. Нужен ли CI в репозитории (self-check'и на x86 без моделей), или проверки
    только руками на плате?
12. Под каким пользователем systemd-юниты и кто владеет `/srv/asr` — root или
    отдельный `asr`?

## B. Данные, глоссарий, эталон

13. **[БЛОКЕР]** Есть ли уже запись созвона на 20–30 мин и размеченный вручную
    эталон на 2 мин? Если нет — кто и когда размечает? Без них этап 0 не
    стартует.
14. **[БЛОКЕР]** Формат эталона. Для WER хватит текста, но для приёмки этапа 2
    нужен эталон вида `t0 t1 speaker text`. Предлагаю сразу один формат (TSV) на
    оба замера, чтобы не размечать дважды.
15. `glossary.yaml` существует или составляю я из §1? Порядок величины — 30
    терминов или 300?
16. **[БЛОКЕР]** Структура глоссария: только канонические формы, или канон +
    список ожидаемых искажений (`VDA5050: [вда 5050, вэдэа 50 50, ...]`)? От
    этого зависит и постобработка, и метрика «сколько терминов потеряно».
17. Как термин должен выглядеть в итоговом тексте: всегда латиницей
    (`VDA5050`, `MQTT`)? Если человек сказал «эм-кью-ти-ти» — нормализуем в
    `MQTT`? Что со склонениями («по ADG'у», «в ADG»)?
18. Правила нормализации для WER: регистр, пунктуация, «пять» vs «5», дефисы —
    что считаем ошибкой, а что нет?
19. Метрика по терминам: recall по вхождениям в эталоне (распознан / искажён /
    потерян)? Как считать «VDA 5050» вместо «VDA5050» — как ошибку или как
    попадание?
20. Есть ли архив уже записанных созвонов? Нужен для приёмки `reprocess` на
    десяти созвонах (этап 1.5).

## C. Схема записи, манифест, ingest

21. Кто пишет `manifest.json` — человек руками? Если да, нужен ли
    `asr init <dir>`, генерящий скелет из содержимого каталога? Без него
    манифесты будут битые, а валидацию спека упрощать запрещает.
22. `call_id` задаёт человек или он выводится из имени каталога? Что при
    коллизии с уже обработанным созвоном?
23. В примере `reference: tolk-recording.mp4`, и тот же файл в `solo_tracks` с
    `audio_stream: 1`. Правильно ли, что reference — всегда stream 0 того же
    файла и `audio_stream` для него не задаётся?
24. Если в OBS дорожки перепутаны местами (микрофон в 0, desktop в 1) —
    проверять эвристикой или полностью доверять манифесту?
25. `attendees` — это `speaker_id` из `speakers`. Что делать, если в списке есть
    id без enrollment: ошибка ingest, предупреждение, или просто не участвует в
    идентификации?
26. Часовые пояса: `started_at` со смещением. В БД хранить как есть или
    приводить к UTC? Что писать в `created_at`?
27. Что ingest делает с исходниками — копирует в `calls/<call_id>/`, перемещает,
    оставляет? Удаляем ли исходное аудио после успешного экспорта?
28. Дисковый бюджет NVMe? Нормализованные wav (16k mono s16 ≈ 115 МБ/час на
    дорожку) храним вечно — они нужны для `reprocess`.
29. Битый файл одной дорожки: валим ingest всего созвона или обрабатываем
    остальное с пометкой? §9 требует «внятную ошибку», но не говорит, отменяет
    ли она созвон целиком.
30. Reference отсутствует, сольные есть — отказ (нет таймлайна)? Подтвердите.
31. Два человека записали OBS (две опорные) — правило выбора? Первая в
    манифесте?
32. Толк: кто-то уже проверил фактический формат и путь его локальной записи
    (микс/не микс, частота, контейнер)? Оператор записи под Windows/Mac/Linux?

## D. Этап 0 — замер

33. С какими флагами собирать whisper.cpp (`GGML_NATIVE`, OpenBLAS,
    `-mcpu=native`)? Уже собран на плате или сборка — часть моей работы?
34. Фиксируем тег whisper.cpp? Поведение флагов между релизами меняется.
35. Квантование: спека фиксирует Q5_0. Добавлять Q8_0 в замер (на aarch64
    разница по скорости бывает не в пользу «меньшего» кванта) или строго список
    из §8?
36. `-t 4` — это просто 4 потока или пин на большие ядра (`taskset -c 4-7`)?
    A55 в миксе тянут RTF вниз; мерить оба варианта?
37. `--flash-attn` на CPU-сборке что-то даёт? Проверяем или ставим как в спеке?
38. **[БЛОКЕР]** Мерить RTF на цельном 30-минутном файле или на нарезке по VAD?
    Числа разойдутся кратно (перезагрузка модели на каждый вызов, паддинг
    окна до 30 с). Замер обязан отражать то, как будет устроен этап 1 —
    см. вопрос 47, это один и тот же вопрос.
39. RTF = wall_clock / длительность аудио (>1 — медленнее реального времени)?
    Подтвердите направление, чтобы порог «заметно выше 2.0» читался однозначно.
40. Peak RSS: `VmHWM` из `/proc/<pid>/status` (stdlib) или `/usr/bin/time -v`?
41. Какие `thermal_zone` на этой плате соответствуют SoC/GPU/NPU? Логировать все
    + частоты ядер, чтобы отличить троттлинг от медленной модели?
42. Какой governor выставлен? Замер имеет смысл только в фиксированном режиме.
43. WER считаем своей реализацией Левенштейна (~30 строк stdlib) или разрешён
    `jiwer` на время замера?
44. Решение по итогам этапа 0 (какая модель в tier=1, какая в tier=2) принимаете
    вы по моей таблице, или я предлагаю и двигаюсь дальше сам?
45. Для tier=1 есть ли числовой потолок латентности, ниже которого `small` не
    проходит и надо смотреть streaming zipformer?
46. На станции faster-whisper уже стоит? Версия CUDA/драйвера? Можно ли туда
    ставить пакеты?

## E. Этап 1 — ночной контур

47. **[БЛОКЕР]** Как whisper.cpp получает аудио:
    (а) один запуск на весь файл с его собственным `--vad`;
    (б) отдельный запуск на каждый VAD-сегмент от sherpa-onnx (модель
        перезагружается каждый раз — дорого);
    (в) конкатенация сегментов в чанки ~30 с с картой таймингов.
    Это определяет замер этапа 0, качество таймкодов и весь этап 1.5 (там
    «rsync нарезанных VAD-сегментов» подразумевает (б)/(в)).
48. Параметры VAD (минимальная пауза, паддинг, максимальная длина сегмента,
    склейка коротких) — есть предпочтения или подбираю по эталону?
49. Длинный монолог: резать по внутренним паузам или жёстко по 30 с?
50. Таймкоды: доверять таймингам whisper внутри чанка или привязывать к
    границам VAD? Нужны ли пословные таймкоды (`--dtw`)? Они понадобятся на 2.5
    для маршрутизации и на 2 для назначения говорящего.
51. Язык: жёстко `-l ru` всегда? Что с репликами целиком по-английски?
52. Промпт (224 токена): собирать автоматически из глоссария по полю частоты —
    но как считать токены без токенизатора в stdlib (оценка по символам)? Или
    пусть это будет просто текстовое поле в конфиге, написанное руками?
    Второе честнее и дешевле.
53. Фильтрация галлюцинаций: на русском whisper стабильно выдаёт «Продолжение
    следует...», «Субтитры сделал DimaTorzok», зацикленные повторы. Резать по
    чёрному списку + детектору повторов? Это то, что увидят агенты.
54. Сохранять ли `text_raw` до нормализации глоссария? Иначе не отличить ошибку
    ASR от ошибки подстановки. Схема §7 такого поля не содержит.
55. Порог `difflib.get_close_matches`: подбираю по эталону. Нужен ли стоп-лист
    слов, которые трогать нельзя? Сопоставляем по одиночным словам или по
    n-граммам (термины бывают из двух слов)?
56. Идемпотентность по `(call_id, track, t0)`, где `t0` REAL — сравнение float
    ненадёжно. Предлагаю хранить t0/t1 в миллисекундах INTEGER (или округлять до
    3 знаков и вешать UNIQUE). Это отклонение от схемы в спеке — согласны?
57. Повторный `asr transcribe` на посчитанном созвоне: пропуск, пересчёт с
    заменой, или требовать `--force`?
58. Перечень состояний `calls.status` — ваш или мой
    (`ingested/transcribing/transcribed/exported/failed`)?
59. У `tracks` нет поля статуса, а §8 требует «дорожка помечается, остальные
    считаются». Добавляю `status`/`error` в `tracks`?
60. Экспорт: перезаписываем `<call_id>.jsonl` целиком и атомарно (tmp+rename)?
61. На этапе 1 атрибуции нет — что писать в `speaker` в JSONL: `"unknown"`,
    `null`, или не экспортировать до этапа 2?
62. Логи: stdout/journald достаточно? Нужен ли отдельный прогонный отчёт (время
    по стадиям, температура) файлом?
63. Блокировка: один процесс на систему (flock) или на созвон? `.path` и
    `.timer` могут пересечься.
64. systemd-юниты пишем на этапе 1 или позже? `.path` срабатывает на появление
    файла, а каталог копируется постепенно — нужен маркер готовности. Что
    удобнее команде: класть `manifest.json` последним или трогать `READY`?
65. Во сколько ночной прогон стартует и когда обязан закончиться?

## F. Схема БД и контракт с агентами

66. **[БЛОКЕР]** Как выражается вытеснение? tier=1 → tier=2 читается по `tier`,
    но `reprocess` кладёт новый tier=2 поверх старого tier=2 — по какому полю
    агент поймёт, какая версия актуальна? Нужна колонка (`active`,
    `superseded_by` или `run_id`). Схему §7 можно расширять или она
    зафиксирована?
67. Хранить ли `avg_logprob`/`no_speech_prob` из whisper и similarity-скор
    атрибуции? Первое даёт агентам меру доверия, второе нужно мне для подбора
    порога `unknown`.
68. Хранить ли версию глоссария и промпта в строке сегмента? `reprocess`
    затевается в том числе ради смены глоссария, а `model`+`backend` этого не
    различают.
69. Стабильны ли `segments.id` между перегонами — агенты будут на них ссылаться,
    или их контракт только JSONL?
70. **[БЛОКЕР]** Как агенты узнают, что созвон готов: следят за появлением файла
    в `export/`, читают SQLite, ждут маркер? И читают ли они когда-нибудь
    tier=1?
71. В JSONL `speaker` — это `speaker_id` или `display_name`?
72. **[БЛОКЕР]** Дедупликация в экспорте: речь оператора попадает и в `obs_mic`,
    и в `reference` — два текста на один интервал. Правило: при наличии
    obs_mic/solo подавляем перекрывающиеся сегменты reference? По какому порогу
    перекрытия? Без этого агенты увидят каждую реплику оператора дважды.
73. Миграции схемы: `PRAGMA user_version` + линейные шаги, или на этапе
    прототипа просто пересоздаём БД?
74. Бэкап `db.sqlite` нужен? Куда?

## G. Этап 1.5 — A5000

75. Хост, пользователь, ОС станции; есть ли там venv с faster-whisper или
    разворачиваю я? Станция общая — можно ли менять её окружение?
76. Что едет на станцию: набор посегментных wav или файл целиком? Если целиком —
    у faster-whisper свой VAD и батчинг, и локальная нарезка для GPU-пути не
    нужна вовсе (см. 47).
77. Команда на станции — буквально `python -c "..."`, или разрешено rsync'ом
    класть туда одноразовый скрипт? Состояния он не хранит, демоном не является,
    но формально это файл на той стороне.
78. Какая сеть между платой и станцией? Сжимать ли передачу (flac) — за ночь это
    ~160 МБ сырого.
79. Проверка занятости: `nvidia-smi --query-gpu=memory.free,index`. Если обе
    карты заняты частично — берём максимум и сравниваем с порогом?
    Перепроверять после rsync (за время передачи карту могли занять)?
80. `timeout_sec` относится к чему: к ssh-сессии транскрипции, к rsync, к
    каждому шагу отдельно?
81. Спека говорит «оборвался — пересчитывается созвон целиком» и одновременно
    «созвон может быть частично локальным, частично GPU — это нормально».
    Разрешаю так: уже полученные GPU-сегменты остаются, остаток досчитывается
    локально, `backend` в строках разный. Верно?
82. `reprocess --since` фильтрует по `calls.started_at`? Пересчитывает только
    ASR+глоссарий или всё, включая диаризацию? И нужен ли дешёвый режим
    «переприменить глоссарий к существующему тексту без ASR» — это секунды
    вместо часов и покрывает половину сценариев из §8.
83. Ssh-ключ с `command=` в `authorized_keys` — настраиваю я или это ручная
    работа админа, а я только документирую?

## H. Этап 2 — атрибуция

84. Модель эмбеддингов: 3D-Speaker CAM++, NeMo TitaNet, ERes2Net? Мерить две на
    нашем эталоне или берём одну по вашему выбору?
85. Порог `unknown` подбирается на размеченном созвоне — кто размечает и в каком
    формате (см. 14)?
86. Enrollment: несколько образцов усредняем в один центроид (схема даёт один
    BLOB на человека) или нужна поддержка нескольких эмбеддингов на человека —
    по трактам (гарнитура / телефон / Толк)? §2b прямо предупреждает про
    рассогласование канала, это аргумент за несколько.
87. Автопополнение базы из `obs_mic`: автоматически на каждом созвоне или
    отдельной командой с ручной проверкой? Какой фильтр качества (минимальная
    длительность, отсутствие перекрытия, согласие с текущим центроидом)?
88. **[БЛОКЕР]** Как назначаем говорящего ASR-сегменту — границы диаризации не
    совпадают с границами whisper. Правило «по максимальному перекрытию»
    подходит? Что делать, если сегмент перекрыт двумя говорящими примерно
    поровну: резать текст нельзя, значит `unknown` или большинство?
89. Перекрывающаяся речь (pyannote умеет её размечать) — игнорируем?
90. **[БЛОКЕР]** `--clustering.num-clusters = len(attendees)` конфликтует с
    требованием «кого нет в списке — `unknown`»: если пришёл незаявленный гость
    или кто-то из списка промолчал, точное число кластеров неверно. Что
    приоритетнее — фиксированное число или порог с оценкой?
91. Исключать ли из диаризации интервалы reference, где по VAD дорожки obs_mic
    уже известно, что говорит оператор? И точнее, и дешевле.
92. `display_name` где используется — только в логах?
93. Доля правильной атрибуции для приёмки считается по числу сегментов или по
    времени? По времени честнее (короткие «ага» не должны решать).

## I. Этап 2.5 — сольные дорожки

94. Параметры кросс-корреляции: шаг огибающей (10 мс?), длина окна, минимум окон
    для оценки дрейфа, отбраковка выбросов (медиана или RANSAC). Насколько
    сложную оценку допускаем?
95. Сольная дорожка может начинаться раньше и заканчиваться позже опорной —
    обрезаем по опорному таймлайну?
96. Self-check со сдвигом и растяжением: растягивать через ffmpeg
    (`asetrate`+`aresample`) допустимо, или синтезировать сигнал в numpy?
97. Если корреляция не сошлась (человек молчал весь созвон, дорожка не от того
    созвона) — помечаем дорожку failed и работаем без неё?

## J. Этап 3 — живой контур

98. **[БЛОКЕР]** Откуда плата берёт живой звук? Записывает OBS на машине
    оператора, плата в созвоне не участвует. Нужен транспорт: RTP/ffmpeg-стрим
    с машины оператора, виртуальный кабель, линейный вход — что предполагается?
    В спеке этого нет, а без ответа этап 3 не спроектировать.
99. Как живой контур узнаёт `call_id`, если манифест появляется после встречи?
    Заводим временный созвон и сшиваем ночью, или tier=1 живёт отдельной
    сущностью?
100. Кто потребитель tier=1, если агенты читают только tier=2? Человек? Через
     что он его смотрит?
101. Живой контур — постоянно работающий сервис или запускается на время
     созвона, и кем?

## K. Профилирование, эксплуатация, прочее

102. Писать тайминги по стадиям (ingest / VAD / ASR / диаризация) в БД или в
     лог? Решение по этапу 4 требует доли диаризации в общем времени — дешевле
     собирать это с этапа 1.
103. Требования по хранению и приватности записей (внутренние обсуждения):
     шифрование, срок хранения, кто имеет доступ к `/srv/asr`?
104. NTP на плате есть? `started_at` берём из манифеста или из mtime файлов,
     если поле не заполнили?
105. Формат self-check'ов: одна команда `asr selfcheck`, прогоняющая все
     assert-блоки? Или по одному на модуль?
106. Куда фиксировать результаты приёмки этапов — `RESULTS.md` в репозитории,
     README, issue?
107. Уведомления о падении ночного прогона: journald и всё, или нужно письмо /
     телеграм? Спека запрещает дашборды и метрики, но про уведомления молчит.
