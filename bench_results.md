# Результаты замера (ASR-00), 2026-08-13 — 2026-08-15

Плата: Radxa Rock 5B (rock-5b), RK3588, 8 ядер (4×A76 big + 4×A55 little), 16 ГБ.
Запись: `2026-08-12.mp4`, 970.3с (16м10с), 175 VAD-сегментов, 705.9с речи (73% файла).
Эталон: `gold.tsv` — 45 строк дословно в `wer_window` (0–124.899с, размечено вручную),
остальные 87 строк — только спикер из автотранскрипта Толка, без текста.
`glossary.json` не готов (только черновик `glossary.draft.json` на вычитку владельцу) —
F1 по терминам нигде не посчитан, только RTF и WER.

## Главная находка: на плате физически нет вентилятора

PWM-сигнал на вентилятор подаётся (`/sys/class/hwmon/hwmon7/pwm1`), но крутить нечего.
Идле — 61–64°C по всем термозонам, что уже само по себе много. Под нагрузкой на
4–8 ядрах плата долетает до 108°C (аварийный порог, запас от критических 115°C) за
2–3 минуты. Это ломает предпосылку SPEC.md §2 про «активное охлаждение» и весь расчёт
ночного бюджета (`governor=performance` на 7 ядрах пару часов подряд).

Первая полная матрица (24 конфигурации: small/medium/large-v3-turbo × Q5/Q8 × t4/t8 ×
flash-attn) целиком ушла в перегрев — 0 из 24 конфигураций дошли до конца.

## Механизм защиты (bench.py)

Реализовано без изменений в железе:

1. **Аварийный стоп** при 108°C — конфигурация прерывается, помечается `ПЕРЕГРЕВ`.
2. **Пауза между конфигурациями** — ждём остывания до 70°C (не дольше 600с).
3. **SIGSTOP/SIGCONT посреди сегмента** (главное изменение) — фоновый сэмплер держит
   `whisper-server` на паузе ядра ОС при достижении порога (freeze) и снимает паузу
   после остывания (resume). Реагирует раз в 2с даже посреди сегмента, который может
   считаться минутами у тяжёлых моделей — старая версия (пауза только между
   сегментами) не успевала среагировать. Время заморозки не входит в RTF.

С этим механизмом все прогоны на q8_0-квантах (large-v3-turbo, medium, small×t8)
дошли до конца без единого перегрева — 8 из 8 конфигураций.

## Итоговая таблица — лучшая конфигурация на модель

Везде `t=4`, пин на большие ядра (`taskset -c 4-7`), `flash-attn=on`, квант `q8_0`
(систематически быстрее q5_0/q5_1 — см. «Наблюдения»), с SIGSTOP/SIGCONT:

| модель | RTF | WER | peak RSS |
|---|---|---|---|
| small | **1.320** | 0.449 | 519 МБ |
| medium | 4.581 | 0.399 | 1281 МБ |
| large-v3-turbo | 5.979 | **0.359** | 1089 МБ |

Монотонный компромисс: крупнее модель → точнее (WER падает 45%→40%→36%), но медленнее
(RTF растёт 1.3→4.6→6.0). При бюджете ~1.4ч аудио/сутки (SPEC.md §2) `large-v3-turbo`
даёт ≈8.4ч чистого счёта в сутки — без учёта времени заморозки SIGSTOP (которое на
этой безвентиляторной плате оказалось сопоставимо с самим счётом, см. ниже).

## Все измеренные конфигурации

### small, t=4, полный файл (175 сегментов)

| квант | flash-attn | RTF | WER |
|---|---|---|---|
| q8_0 | on | **1.320** | 0.449 |
| q8_0 | off | 1.512 | 0.475 |

### small, t=8 (все ядра), полный файл, с SIGSTOP/SIGCONT

| квант | flash-attn | RTF | WER |
|---|---|---|---|
| q5_1 | on | 2.770 | 0.460 |
| q5_1 | off | 3.228 | 0.455 |
| q8_0 | on | 1.406 | 0.449 |
| q8_0 | off | 1.669 | 0.460 |

### medium, t=4, полный файл, с SIGSTOP/SIGCONT

| квант | flash-attn | RTF | WER | заморожено (SIGSTOP) |
|---|---|---|---|---|
| q5_0 | on | 9.783 | 0.404 | 7129с (~2ч) |
| q5_0 | off | 12.213 | 0.389 | 4174с |
| q8_0 | on | **4.581** | 0.399 | 3264с |
| q8_0 | off | 5.546 | 0.404 | 3358с |

### large-v3-turbo, t=4, полный файл, с SIGSTOP/SIGCONT

| квант | flash-attn | RTF | WER | заморожено (SIGSTOP) |
|---|---|---|---|---|
| q8_0 | on | **5.979** | **0.359** | 5737с (~1.6ч) |
| q8_0 | off | 7.854 | 0.379 | 5113с |
| q5_0 | on/off | не досчитана | — | — |

`q5_0` для `large-v3-turbo` **не досчитана до конца намеренно**: на 20/175 сегментах
ETA выросла до 7ч на одну конфигурацию при RTF~20–27 — то же самое, что показывали
частичные данные раньше (RTF~20–30). Практическая непригодность уже дважды
подтверждена (здесь и на `medium`, где q5_0 тоже в разы медленнее q8_0), досчитывать
до конца не было смысла — остановил, чтобы не тратить ещё 12+ часов на заведомо
нежизнеспособный вариант.

## Замер на etu-pc-1 (A5000): полная матрица

`faster-whisper` (`ctranslate2` 4.8.1), fp16, батчинг (`BatchedInferencePipeline`),
VAD станции, дорожка целиком без нашей нарезки (SPEC.md §9, этап 1.5). venv и
скрипты остались на станции в `/root/asr-bench-gpu/`; сам скрипт —
`scripts/gpu_bench_full.py`, WER считается на плате той же реализацией
нормализации, что и для платы.

Настройка доступа: ключ без пароля сгенерирован на плате, добавлен владельцем в
`authorized_keys` root на etu-pc-1 вручную (по инструкции SPEC.md §9). Риск про
CUDA 13 не подтвердился — драйвер 595.71.05 обратно совместим, обычное pip-колесо
`ctranslate2` (собрано под CUDA 12) увидело GPU без танцев с бубном.

WER — в том же `wer_window`, что и на плате; «+гл» — после нормализации
глоссарием; F1 — по терминам глоссария в окне.

| модель | beam | RTF | WER | WER +гл | F1 |
|---|---|---|---|---|---|
| large-v3 | 5 | 0.0126 | 0.303 | **0.293** | 1.00 |
| large-v3 | 1 | 0.0097 | 0.313 | 0.313 | 0.67 |
| large-v3-turbo | 5 | 0.0060 | 0.328 | 0.318 | 1.00 |
| large-v3-turbo | 1 | 0.0056 | 0.333 | 0.323 | 1.00 |
| medium | 5 | 0.0078 | 0.354 | 0.348 | 0.67 |
| medium | 1 | 0.0063 | 0.404 | 0.399 | 0.67 |
| small | 5 | 0.0041 | 0.379 | 0.374 | 0.86 |
| small | 1 | 0.0034 | 0.364 | 0.359 | 0.86 |

Станция быстрее платы на три порядка: `large-v3` beam=5 даёт RTF 0.0126 против
5.979 у лучшего варианта платы — 970-секундная запись считается за 12 секунд
против полутора часов чистого счёта (плюс полтора часа заморозки на охлаждение).
Перегон всего архива, ради которого этап 1.5 и задумывался (SPEC.md §2), на
станции реален: сутки записей — минуты счёта.

По качеству: лучший результат станции (`large-v3` beam=5, WER 0.293 с глоссарием)
против лучшего на плате (`large-v3-turbo/q8_0/on`, WER 0.359) — снижение WER на
~18% относительных. Заметно, но не радикально.

**Оговорка про критерий этапа 1.5.** SPEC.md требует сравнивать не WER, а
терминологию: «если large-v3 beam=5 убивает хотя бы вдвое меньше терминов
глоссария, чем turbo на плате — делать». В размеченном окне этой записи всего
**4 вхождения терминов** (`LOC`×3, `XR`×1), и на такой базе F1 скачет от 0.67 до
1.00 из-за одного-двух слов — статистически это ни о чём. Терминов из домена
(VDA5050, MAPF, Isaac Sim, EECBS) в записи нет вообще: созвон был про
инфраструктуру, а не про алгоритмы. **Критерий SPEC на этих данных не
оценивается**; нужна запись, где предметная терминология реально звучит.

Первая попытка посчитать WER (по границам сегментов VAD станции, без пословной
обрезки) давала WER 0.4646 — хуже платы. Причина: VAD `faster-whisper` даёт
намного более крупные сегменты (31 против 175 у sherpa-onnx), один из них
(115.55–140.06с) вылезал за `wer_window` (124.899с) и тащил в сравнение лишний
текст. Пересчитал с `word_timestamps=True` и обрезкой по словам — WER упал до
0.303. Это подтверждает то, что уже написано в SPEC.md §2.5 про важность
единой методики нарезки для сопоставимости цифр.

## Глоссарий

`glossary.json` собран итеративно из девяти независимых расшифровок одной записи
(Контур.Толк + восемь прогонов разных моделей на станции). Инструмент сбора —
`scripts/glossary_candidates.py`, сигнал ровно из SPEC.md §7.2: латиница плюс
слова, которые пишутся по-разному в одинаковых контекстах.

Что потребовалось, чтобы сигнал стал полезным:

- **Транслитерация при сравнении.** «лок» и `LOC` побуквенно не похожи вообще,
  difflib их никогда не свяжет. Сравнение идёт по транслитерированному ключу —
  тогда `нетборд`↔`netbird` даёт 0.86, `Юнити`↔`Unity` 0.80, а несвязанные слова
  остаются врозь (`привет`↔`дейлик` 0.17).
- **Узкое окно контекста.** При окне 30с в один кластер сваливались склонения из
  разных предложений («задача/задачи/задаче») — по SPEC они снимаются
  автоматически и в `variants` не нужны. Окно 4с оставляет только реальный
  разнобой моделей в одном месте записи.
- **Порог длины.** На словах в три буквы difflib слишком лоялен: «как», «так» и
  «там» отличаются одной буквой из трёх и слипаются в шум из служебных слов.
  Кириллица берётся от 5 букв, латиница — от 2 (аббревиатуры и есть цель).

Найденное таким образом и вошедшее в глоссарий: `SSH` (искажения «ссаш», «эсаш»,
«сш»), `VPN`, `WebRTC` («артиси», «эртиси», `WBRTC`), `NetBird` («нетборд»,
«нетбертиш»), `WireGuard` («вгариде», «вай гор»), `TLS` («тлса»), `Docker`,
`Unity` («юнутри», `Winitry`), `GitHub` («гитте»), `README` («выдме», «эдме»),
`iptables` («бибайпитейблсом»), плюс префиксы задач команды `ASR`, `XR`, `LOC`,
`PUR`, `LEARN`, `ROUTINE`. Термины из SPEC.md (`VDA5050`, `MAPF`, `EECBS`, `ADG`,
`Isaac Sim`, `RKNN`, `MQTT`) оставлены на будущие созвоны — в этой записи их нет.

### Ложные срабатывания: порог пришлось поднимать

Первая версия глоссария при пороге 0.8 портила обычную речь, и это ловится
только проверкой на реальном тексте, не на юнит-тестах:

| было | становилось | сколько раз |
|---|---|---|
| `надо` | `NetBird` | 9 |
| `Саш`, `саш` | `SSH` | 3 |
| `внутри` | `Unity` | 1 |
| `крути` | `ROUTINE` | 1 |
| `ло`, `пр` | `LOC`, `PUR` | 2 |

Имя человека, превращающееся в сетевой протокол, — ровно тот случай, ради
которого SPEC.md §9.1 запрещает LLM-проход для нормализации («он поправит и
правильное тоже»); словарная замена ошибается не менее охотно, просто
предсказуемее. Исправлено тремя правками, каждая закрыта тестом-регрессией:
точное совпадение с вариантом срабатывает всегда, нечёткое — только на словах от
4 букв, порог поднят с 0.8 до 0.85, самый опасный вариант («надбор») убран.
После этого на всей записи остались только осмысленные подстановки: `SSH`×4,
`VPN`×4, `LOC`×3, дальше по одной-две.

## Промпт с глоссарием ухудшает WER

SPEC.md §9.1 задаёт два эшелона защиты терминологии: глоссарий в `--prompt`
whisper.cpp и словарная постобработка. Замер показал, что **первый эшелон
работает против общего качества**.

`large-v3-turbo/q8_0/t=4/fa=on`, одна и та же запись и окно:

| вариант | RTF | WER | F1 |
|---|---|---|---|
| без промпта, без постобработки | 5.979 | **0.359** | — |
| с промптом, до постобработки | 6.497 | 0.399 | — |
| с промптом + постобработка | 6.497 | 0.399 | 0.75 |

Постобработка не меняет WER вообще (0.399 → 0.399): она трогает единицы слов,
на общем счёте это незаметно — и это нормально, её ценность в F1.

А вот промпт из 22 терминов поднял WER на 4 п.п. Видно, что именно ломается:
модель начинает «слышать» технику там, где её нет.

| эталон | без промпта | с промптом |
|---|---|---|
| «Лок то в итоге — это че, Сашин?» | «Лок то в итоге это что, Сашин?» | «Кстати, **Лоб, ты что**, Сашин?» |
| «а че не поставил?» | «Ничего не поставил» | «**Чуть** не поставил» |
| «у нас же задачи двигать надо» | «Ой, блин, наши задачи двигать надо» | «наша задача **прибить** надо» |

Польза от промпта тоже есть: с ним модель сама пишет `LOC` латиницей, без
него — «Лок» кириллицей. То есть промпт смещает распределение в сторону
терминов: выигрыш на терминах, проигрыш на всём остальном.

Это ровно та же ловушка, из-за которой SPEC.md §9.1 запрещает LLM-проход для
нормализации («он поправит и правильное тоже»), только здесь она в первом
эшелоне, а не в третьем. Промпт стоит либо сокращать до нескольких самых
частотных терминов, либо не использовать вовсе, полагаясь на постобработку —
цифры для решения даёт таблица выше, дополняется прогоном «без промпта +
постобработка».

## Полный gold.tsv

`scripts/make_gold_tsv.py` собирает эталон из расшифровки Толка, **не трогая
ручную разметку внутри `wer_window`** — это выверенный владельцем текст, и
затирать его скриптом нельзя. Всё, что после окна (95 реплик), берётся из Толка:
время начала — её, конец подрезается по VAD, `speaker_id` приводится к латинице
строчными по SPEC.md §7.3 (`andrey`, `pavel`, `danila`, `artem`, `alexander`).

Зачем подрезать конец по VAD: у Толка есть только время начала реплики, поэтому
наивное «до следующей реплики» растягивает короткое «угу» на полминуты тишины —
в Audacity такие метки неудобно править. Заодно всплыло, что у Толка бывают две
реплики с одной и той же секундой (перебивание) — такие метки схлопывались в
нулевую длину, теперь для них ограничение сверху снимается (SPEC.md §7.3
допускает перекрывающиеся интервалы, а нулевой длины — нет).

Результат — `/opt/gold_full.tsv`, 137 сегментов (42 ручных + 95 из Толка). Рядом
`--audacity-out` кладёт `/opt/gold_full.audacity.txt` — то же самое без строк
`# call_id` / `# wer_window`: Audacity при импорте меток ждёт число в первой
колонке и на комментариях спотыкается, а формат из SPEC.md §7.3 их требует.
Править надо этот файл, после чего `--from-labels` соберёт `gold.tsv` обратно
вместе с шапкой (round-trip проверен — содержимое совпадает побайтно). **Это черновик для правки, а не эталон**: расшифровка
Толка сама с ошибками (см. таблицу искажений выше — «Эсаш», «Нетбертишь»,
«Бибайпитейблсом»). Для WER по-прежнему берётся только `wer_window`; когда
владелец вычитает остальное, окно можно будет расширить и мерить на большем
объёме.

## Наблюдения

- **q8_0 систематически быстрее q5_0/q5_1** того же класса модели, иногда в разы
  (large-v3-turbo: ~25 → ~6–8; medium: ~11 → ~5). Вероятная причина — whisper.cpp на
  этом CPU (`cortex-a76.cortex-a55+dotprod`) репакует тензоры под NEON
  (`q8_0_4x4`) для q8_0, а для q5_0/q5_1 такой быстрой раскладки, похоже, нет.
- **`t=8` не быстрее `t=4` с пином на большие ядра**, местами чуть медленнее
  (small/q8_0/on: 1.406 против 1.320). Little-ядра (A55), видимо, либо тормозят
  синхронизацию, либо не успевают полезно подключиться. `t=4` + `taskset -c 4-7`
  — либо лучший, либо не худший вариант, и явно экономичнее по теплу.
- **WER не зависит от потоков/flash-attn** (как и должно быть — это параметры
  производительности, не качества): 0.449 у small/q8_0/on что на t=4, что на t=8.
- **WER 29–48%** высокий по абсолютной величине даже на станции — это разговорная
  речь с перебиваниями и обрывами фраз, а не диктовка. Нижняя граница (0.293 у
  `large-v3` beam=5 с глоссарием) — то, что вообще достижимо на этом материале.
- **Глоссарий даёт около половины процентного пункта WER** (например 0.303 → 0.293)
  — на общем WER он почти не виден и не должен: терминов в тексте единицы. Его
  ценность в другом, в F1 по этим единицам, ради которых агенты и читают
  транскрипт: «Эсаш» вместо `SSH` ломает смысл, а на WER не влияет.
- **Заморозка SIGSTOP съедает столько же времени, сколько сам счёт**, а то и больше
  (у large-v3-turbo/on — 5737с заморозки против ~610с чистого счёта на 175 сегментов
  при RTF~6). Без вентилятора реальное настенное время ночного прогона будет заметно
  больше, чем подсказывает один RTF — это отдельная метрика, которую стоит завести
  в `timings` на этапе 1 (SPEC.md §8: «timings заполняется на каждой стадии»).

## Что не сделано

- **Критерий этапа 1.5 из SPEC.md по-прежнему не оценён.** F1 считается, но база —
  4 вхождения терминов в размеченном окне, на ней метрика скачет от одного слова.
  Терминов из домена (VDA5050, MAPF, EECBS, Isaac Sim) в записи нет вообще. Нужна
  запись, где предметная терминология звучит, и вычитанный `gold.tsv` пошире.
- **`glossary.json` требует вычитки владельцем.** Собран автоматически по SPEC.md
  §7.2 и проверен на ложные срабатывания, но часть кандидатов я не смог трактовать
  без знания предметной области и в глоссарий не внёс: `SAC`/`САК`/`ASSAC`,
  `PC3`/`PC5` («ла Пии 3», «писи 5»), `Monogia`, `Vita`, `Vapor`, `StartStreaming`,
  «палатности», «монагер/манагер». Список кандидатов воспроизводится командой:
  `python3 scripts/glossary_candidates.py --tolk <толк.md> --gpu-json <json> --window-s 4`
- **Полный `gold.tsv` — черновик**, вычитка в Audacity за владельцем.
- Решение по модели для tier=1/tier=2 и по этапу 1.5 принимает владелец по этим
  таблицам (SPEC.md §9, Этап 0) — здесь не зафиксировано.
